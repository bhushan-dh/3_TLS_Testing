{
  "name": "node-app",
  "version": "1.0.0",
  "description": "Node.js app that makes calls to non-TLS and TLS services, as well as an AWS service.",
  "main": "index.js",
  "dependencies": {
    "http": "^0.0.1-security",
    "https": "^1.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  }
}
